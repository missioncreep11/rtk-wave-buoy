#ifndef SECRETS_POLARIS_EXAMPLE_H
#define SECRETS_POLARIS_EXAMPLE_H

// Point One Polaris — copy these fields into secrets.h for LTE experiments.
// Use class credentials from the Canvas assignment; do not commit real keys.
//
// Full context: documentation/polaris-exploration.md
// WiFi reference client: esp32/legacy/esp32_polaris_wifi.ino

const char *casterHost    = "polaris.pointonenav.com";
const uint16_t casterPort   = 2101;
const char *mountPoint      = "POLARIS";
const char *casterUser      = "<your class email or account id>";
const char *casterUserPW    = "<your class API key from Canvas>";

// Hologram Cloud Socket (unchanged for buoy_combo telemetry)
const char hologramDeviceKey[] = "";

#ifndef TELEMETRY_INTERVAL_MS
#define TELEMETRY_INTERVAL_MS 60000UL
#endif

// Optional: select Polaris NTRIP profile when ntrip_profile.h is restored
// #define NTRIP_PROFILE_POLARIS 1

#endif
