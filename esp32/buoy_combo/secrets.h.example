#ifndef SECRETS_H
#define SECRETS_H

const char *casterHost   = "132.239.117.27";
const uint16_t casterPort = 2101;
const char *mountPoint   = "BASE_CCS";
const char *casterUser   = "your_user";
const char *casterUserPW = "your_password";

// --- Telemetry (pick one) ---

// Local dashboard via ngrok:
//   See documentation/local-portal.md
//   1) python local_portal/server.py
//   2) ngrok http 8080
//   3) paste https URL below + /api/ingest
//   Leave telemetrySecret empty.
#define HAS_TELEMETRY_URL 1
const char *telemetryUrl = "https://YOUR-ID.ngrok-free.app/api/ingest";
#define HAS_TELEMETRY_SECRET 1
const char *telemetrySecret = "";

// Or GitHub Pages via Cloudflare Worker:
//   See documentation/github-pages.md
//   Set telemetryUrl to your Worker URL and telemetrySecret to match BUOY_SECRET.
// const char *telemetryUrl = "https://rtk-buoy-proxy.YOUR_USERNAME.workers.dev";
// const char *telemetrySecret = "your-buoy-secret";

// Or Hologram Cloud socket only (dashboard Activity tab):
//   Clear telemetryUrl, set hologramDeviceKey from Hologram Router Credentials.
#define HAS_HOLOGRAM_DEVICE_KEY 1
const char hologramDeviceKey[] = "";

#ifndef TELEMETRY_INTERVAL_MS
#define TELEMETRY_INTERVAL_MS 60000UL
#endif

#endif
